---
description: "Use the project impeccable skill with its built-in routing rules."
argument-hint: "[command or target]"
---

Use the project skill at `.claude/skills/impeccable`.

Apply the routing rules from `.claude/skills/impeccable/SKILL.md` to this exact input:

`$ARGUMENTS`

If the first word matches an impeccable sub-command, follow that reference file. Otherwise, treat the whole input as a general impeccable request.

